如果用new关键字声明一个对象的话，那么其本身也是一个函数

test() 方法用于检索字符串中的指定值 --- 简言之 就是是否有我要查找的字符串

exec() 方法检索字符串中的指定值 若果找到 则返回匹配的值 没有找到则返回NULL

字符串的match

g 即时全局匹配 又忽略大小写

str.search(//) 找到则返回在字符串中的位置 有的话返回其数值 没有的话返回-1

str.match() 字符串匹配 返回匹配的结果

str.replace(//, "v"); 字符串替换

str.split(//) 字符串分割

主要用于 注册登录检测 过滤关键字
